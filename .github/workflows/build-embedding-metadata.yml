name: Build Embedding Metadata

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-embedding-metadata:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pyyaml

      - name: Build embedding metadata
        run: python scripts/build_embedding_metadata.py

      - name: Validate TX files
        run: python scripts/validate_tx.py

      - name: Build tx_index.json
        run: python scripts/build_index.py

      - name: Build public_tx_index.json
        run: python scripts/filter_public_index.py

      - name: Audit metadata
        run: python scripts/audit_metadata.py

      - name: Commit embedding metadata
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: auto build embedding metadata
